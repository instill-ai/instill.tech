import { DocsLayout } from "@/components/docs";
export const meta = {
  title: "VDP Cloud",
  lang: "en-US",
  draft: false,
  description: "",
};

VDP Cloud is a fully-managed public cloud service for the VDP project. Access to VDP cloud is currently by invitation only. You can [join the wait list](https://www.instill.tech/get-access).

## Private alpha

export default ({ children }) => (
  <DocsLayout meta={meta}>{children}</DocsLayout>
);
