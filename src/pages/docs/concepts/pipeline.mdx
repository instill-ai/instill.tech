import { DocsLayout } from "@/components/docs";
export const meta = {
  title: "Pipeline",
  lang: "en-US",
  draft: true,
  description: "",
  date: "",
};

export default ({ children }) => (
  <DocsLayout meta={meta}>{children}</DocsLayout>
);

# Pipeline Mode

![Pipeline modes](/docs-assets/pipeline-modes.png)
