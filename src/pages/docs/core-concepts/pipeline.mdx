import { DocsLayout } from "@/components/docs";
export const meta = {
  title: "Pipeline",
  lang: "en-US",
  draft: false,
  description:
    "Build end-to-end unstructured visual data pipelines with the open-source visual data ETL tool VDP https://github.com/instill-ai/vdp",
  date: "",
};

export default ({ children }) => (
  <DocsLayout meta={meta}>{children}</DocsLayout>
);

## Pipeline Mode

![Pipeline modes](/docs-assets/pipeline-modes.png)
