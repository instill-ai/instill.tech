import { DocsLayout } from "@/components/docs";
export const meta = {
  title: "Model",
  lang: "en-US",
  draft: false,
  description: "One-click import and deploy ML models with the open-source visual data ETL tool VDP https://github.com/instill-ai/vdp",
  date: "",
};

export default ({ children }) => (
  <DocsLayout meta={meta}>{children}</DocsLayout>
);
